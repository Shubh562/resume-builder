# Resume Builder

Single-page resume builder that exports the resume in the provided format as a
downloadable PDF.

## Local setup

```bash
npm install
npm run dev
```

Then open the URL printed in the terminal. Use the **Download PDF** button to
export the resume.

